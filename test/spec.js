// import { getCountyByPhoneNumber } from './countryManager';

var Code = require('code');
var Lab = require('lab');
var lab = exports.lab = Lab.script();

var describe = lab.describe;
var it = lab.it;
var before = lab.before;
var after = lab.after;
var expect = Code.expect;

describe('countryManager', function () {

    it('getCountyByPhoneNumber()', function (done) {

        // TODO use es6
        // var phoneNumber_PT = '+351920000000';
        // expect(getCountyByPhoneNumber(phoneNumber_PT)).to.equal('Portugal');
        done();
    });
});
